name: Automatically create pull request

on:
  push:
    branches:
      - db-failover

jobs:
  open-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      repository-projects: write
    steps:
      - uses: actions/checkout@v3
      - run: |
          gh pr create -B main -t 'db failover' -b 'body'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
